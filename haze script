-- Haze Seas - Hub Complet (Catégories + Foldable Farm + Settings)
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")

local player = Players.LocalPlayer

-- Variables
local autoFarm = false
local selectedNPC = nil
local flying = false
local noclipping = false
local killAura = false

-- ==================== GUI PRINCIPALE ====================
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "HazeHub"
screenGui.ResetOnSpawn = false
screenGui.Parent = player:WaitForChild("PlayerGui")

local main = Instance.new("Frame")
main.Size = UDim2.new(0, 460, 0, 620)
main.Position = UDim2.new(0.5, -230, 0.5, -310)
main.BackgroundColor3 = Color3.fromRGB(18, 18, 25)
main.Parent = screenGui
Instance.new("UICorner", main).CornerRadius = UDim.new(0, 16)

-- Drag
local dragging = false
main.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = main.Position
    end
end)
main.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end
end)
UserInputService.InputChanged:Connect(function(input)
    if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStart
        main.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

-- Titre
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1,0,0,60)
title.BackgroundColor3 = Color3.fromRGB(255, 130, 40)
title.Text = "🍋 Haze Seas Hub"
title.TextColor3 = Color3.new(1,1,1)
title.Font = Enum.Font.GothamBold
title.TextSize = 24
title.Parent = main
Instance.new("UICorner", title).CornerRadius = UDim.new(0, 16)

-- Sidebar Catégories
local sidebar = Instance.new("Frame")
sidebar.Size = UDim2.new(0, 140, 1, -70)
sidebar.Position = UDim2.new(0, 10, 0, 70)
sidebar.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
sidebar.Parent = main
Instance.new("UICorner", sidebar).CornerRadius = UDim.new(0, 12)

local content = Instance.new("ScrollingFrame")
content.Size = UDim2.new(1, -160, 1, -80)
content.Position = UDim2.new(0, 160, 0, 70)
content.BackgroundTransparency = 1
content.ScrollBarThickness = 6
content.Parent = main

-- Catégories
local categories = {"Farm", "Combat", "Misc", "Paramètres"}
local currentCategory = "Farm"

for i, cat in ipairs(categories) do
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, -20, 0, 50)
    btn.Position = UDim2.new(0, 10, 0, (i-1)*60)
    btn.BackgroundColor3 = Color3.fromRGB(35,35,50)
    btn.Text = cat
    btn.TextColor3 = Color3.new(1,1,1)
    btn.Font = Enum.Font.GothamSemibold
    btn.Parent = sidebar
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 10)

    btn.MouseButton1Click:Connect(function()
        currentCategory = cat
        -- Load content for category (simplified for now)
        print("Catégorie chargée : " .. cat)
    end)
end

-- Farm Section (pliable)
local farmSection = Instance.new("Frame")
farmSection.Size = UDim2.new(1, -20, 0, 300)
farmSection.Position = UDim2.new(0, 10, 0, 10)
farmSection.BackgroundColor3 = Color3.fromRGB(30,30,40)
farmSection.Parent = content
Instance.new("UICorner", farmSection).CornerRadius = UDim.new(0, 12)

-- Ajoute ici les NPC (exemple)
local npcs = {"Bandit", "Orc", "Wolf", "Boss"}

for i, npc in ipairs(npcs) do
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, -20, 0, 40)
    btn.Position = UDim2.new(0, 10, 0, (i-1)*50)
    btn.BackgroundColor3 = Color3.fromRGB(40,40,55)
    btn.Text = npc
    btn.TextColor3 = Color3.new(1,1,1)
    btn.Parent = farmSection
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)

    btn.MouseButton1Click:Connect(function()
        selectedNPC = npc
        print("NPC sélectionné : " .. npc)
    end)
end

print("✅ Hub Haze Seas chargé - Tout est inclus")
